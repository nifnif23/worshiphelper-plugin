# WorshipHelper Python server dependencies -- v7
# Install: pip install -r requirements.txt
#
# GPU (RTX 3050 / any CUDA 11.8+):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# CPU only:
#   pip install -r requirements.txt
#   (torch CPU wheel is pulled automatically as a transitive dep of silero-vad)

# --- Core STT ---------------------------------------------------------------
# faster-whisper: CTranslate2-based Whisper. distil-large-v3 needs >= 1.0.3.
faster-whisper>=1.0.3

# --- Transport ---------------------------------------------------------------
websockets>=12.0,<14.0

# --- VAD ---------------------------------------------------------------------
# Silero VAD runs on torch. The CUDA wheel must be installed separately
# (see above) before this file if you want GPU VAD.
torch>=2.2,<3.0

# --- Semantic search (embedding model) --------------------------------------
# sentence-transformers hosts BAAI/bge-base-en-v1.5 (768d).
# Upgrading from all-MiniLM-L6-v2 (384d) — REQUIRES rebuilding verses.sqlite.
sentence-transformers>=3.0.0
numpy>=1.26,<3.0

# --- Embed HTTP server -------------------------------------------------------
aiohttp>=3.9,<4.0

# --- Linux/macOS event loop speedup (no-op on Windows) ----------------------
uvloop==0.19.0 ; platform_system != "Windows"
